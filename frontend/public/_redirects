# 静态资源直接命中（避免被 SPA fallback 误吞）
/assets/*               /assets/:splat             200
/bg-glass-butterfly.png /bg-glass-butterfly.png    200
/*.png                  /:splat.png                200
/:file.html             /:file.html                200

# SPA fallback：其余路由返回 index.html
/*                      /index.html                200
